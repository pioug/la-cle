name: La clé
description: Configure an SSH key for private GitHub dependencies on Linux, macOS, and Windows

inputs:
  GH_SSH_KEY:
    description: Private SSH key used to authenticate with GitHub
    required: true

runs:
  using: node24
  main: index.js
